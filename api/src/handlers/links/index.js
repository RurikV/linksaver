const { addLink } = require("./add-link");
const { getLinks } = require("./get-links");
const { updateLink } = require("./update-link");
const { deleteLink } = require("./delete-link");

module.exports = {
  addLink,
  getLinks,
  updateLink,
  deleteLink,
};
